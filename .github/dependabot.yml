version: 2
updates:
  # Keep the SHA-pinned GitHub Actions in .github/workflows current (security patches).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
